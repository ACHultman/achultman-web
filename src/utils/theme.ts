/**
 * Returns rgba string with primary green color and alpha value
 * @param alpha the alpha value (0-1)
 * @returns rgba string
 */
export function rgbaGreen(alpha: number): string {
    return `rgba(56, 161, 105, ${alpha})`;
}
